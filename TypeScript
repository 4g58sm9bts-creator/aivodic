import React from 'react';

// Tipovi za lekcije
const lessons = [
  { id: 1, title: "Uvod u LLM", desc: "Nauči kako rade modeli poput GPT-4.", tag: "Teorija" },
  { id: 2, title: "Prompt Engineering", desc: "Tehnike za bolje rezultate.", tag: "Praksa" },
  { id: 3, title: "Fine-tuning", desc: "Prilagođavanje modela tvojim podacima.", tag: "Napredno" },
];

export default function GuidePage() {
  return (
    <div className="min-h-screen bg-slate-950 text-white font-sans">
      {/* Hero sekcija */}
      <header className="py-20 px-6 text-center border-b border-slate-800 bg-gradient-to-b from-blue-900/20 to-transparent">
        <h1 className="text-5xl font-extrabold tracking-tight mb-4">
          Mastering <span className="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">AI Intelligence</span>
        </h1>
        <p className="text-slate-400 max-w-2xl mx-auto text-lg">
          Ultimativni vodič kroz umjetnu inteligenciju. Od nule do inženjera.
        </p>
      </header>

      {/* Grid s lekcijama */}
      <main className="max-w-6xl mx-auto py-16 px-6 grid md:grid-cols-3 gap-8">
        {lessons.map((lesson) => (
          <div key={lesson.id} className="group relative p-8 rounded-2xl bg-slate-900 border border-slate-800 hover:border-blue-500 transition-all duration-300">
            <div className="absolute -inset-0.5 bg-gradient-to-r from-blue-500 to-cyan-500 rounded-2xl blur opacity-0 group-hover:opacity-20 transition duration-300"></div>
            <span className="text-xs font-bold uppercase tracking-widest text-cyan-400">{lesson.tag}</span>
            <h3 className="text-xl font-bold mt-4 mb-2">{lesson.title}</h3>
            <p className="text-slate-400 leading-relaxed">{lesson.desc}</p>
            <button className="mt-6 text-sm font-semibold flex items-center text-blue-400 hover:text-blue-300">
              Započni učenje →
            </button>
          </div>
        ))}
      </main>
    </div>
  );
}
